# Core framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database and ORM
SQLAlchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10  # PostgreSQL driver (use if you're using Postgres)
# or install another driver, e.g. sqlite requires no extra dependency

# Data validation and serialization
pydantic==2.9.2
pydantic-settings==2.4.0

# PDF and DOCX parsing
pypdf==5.1.0
python-docx==1.1.2

# File handling
python-multipart==0.0.9

# Embeddings or vector DB (adjust depending on what add_resume_to_vector_db uses)
openai==1.52.2       # For embeddings using OpenAI API
    # For local vector similarity search
# or chromadb==0.5.5  # Optional if using Chroma as vector DB

# Utilities
requests==2.32.3
tqdm==4.66.5

# Development & environment
python-dotenv==1.0.1

google-generativeai==0.3.2

# --------------------
# AI / Embedding & Vector DB
# --------------------
sentence-transformers==3.0.1
chromadb==0.5.11
faiss-cpu==1.12.0          # For similarity backend (if Chroma uses FAISS)
numpy==1.26.4
torch==2.4.1
transformers==4.44.2
accelerate==0.32.1